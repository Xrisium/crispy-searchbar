<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:CrispySearchbar.ViewModels"
        xmlns:modes="using:CrispySearchbar.Core.Modes"
        xmlns:converters="using:CrispySearchbar.Converters"
        x:Class="CrispySearchbar.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Width="660"
        Height="88"
        WindowStartupLocation="CenterScreen"
        WindowDecorations="None"
        CanResize="False"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        TransparencyLevelHint="Transparent"
        Title="Crispy Searchbar"
        FontFamily="Segoe UI, Microsoft YaHei UI">
  <Window.Resources>
    <converters:ModeIconTemplateConverter x:Key="ModeIconTemplateConverter" />
  </Window.Resources>
  <Panel>
    <Border x:Name="CapsuleBorder"
            Margin="10,8,10,26"
            Height="54"
            CornerRadius="27"
            Background="{DynamicResource SearchSurfaceBrush}"
            BorderBrush="{DynamicResource SearchSurfaceBorderBrush}"
            BorderThickness="1"
            BoxShadow="0 8 22 0 #38000000"
            PointerPressed="OnCapsulePointerPressed"
            PointerWheelChanged="OnCapsulePointerWheelChanged">
      <Grid ColumnDefinitions="*,Auto">
        <TextBox x:Name="QueryBox"
                 Grid.Column="0"
                 Margin="0,0,8,0"
                 Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 PlaceholderText="{Binding CurrentMode.Watermark}"
                 Background="Transparent"
                 FontSize="20"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 CaretBrush="{DynamicResource AccentBrush}"
                 SelectionBrush="{DynamicResource SelectionBrush}"
                 Padding="20,0,8,0"
                 VerticalContentAlignment="Center">
          <TextBox.Template>
            <ControlTemplate TargetType="TextBox">
              <Border Background="Transparent" ClipToBounds="True">
                <DockPanel Margin="{TemplateBinding Padding}">
                  <ScrollViewer x:Name="PART_ScrollViewer"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden">
                    <Panel>
                      <TextBlock x:Name="PART_Placeholder"
                                 Text="{TemplateBinding PlaceholderText}"
                                 Foreground="{DynamicResource TextPlaceholderBrush}"
                                 TextTrimming="CharacterEllipsis"
                                 HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                 VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                        <TextBlock.IsVisible>
                          <MultiBinding Converter="{x:Static BoolConverters.And}">
                            <Binding ElementName="PART_TextPresenter" Path="PreeditText" Converter="{x:Static StringConverters.IsNullOrEmpty}" />
                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Text" Converter="{x:Static StringConverters.IsNullOrEmpty}" />
                          </MultiBinding>
                        </TextBlock.IsVisible>
                      </TextBlock>

                      <TextPresenter x:Name="PART_TextPresenter"
                                     Text="{TemplateBinding Text, Mode=TwoWay}"
                                     CaretBlinkInterval="{TemplateBinding CaretBlinkInterval}"
                                     CaretIndex="{TemplateBinding CaretIndex}"
                                     SelectionStart="{TemplateBinding SelectionStart}"
                                     SelectionEnd="{TemplateBinding SelectionEnd}"
                                     TextAlignment="{TemplateBinding TextAlignment}"
                                     TextWrapping="{TemplateBinding TextWrapping}"
                                     LineHeight="{TemplateBinding LineHeight}"
                                     LetterSpacing="{TemplateBinding LetterSpacing}"
                                     SelectionBrush="{TemplateBinding SelectionBrush}"
                                     SelectionForegroundBrush="{TemplateBinding SelectionForegroundBrush}"
                                     CaretBrush="{TemplateBinding CaretBrush}"
                                     HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                     VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Panel>
                  </ScrollViewer>
                </DockPanel>
              </Border>
            </ControlTemplate>
          </TextBox.Template>
        </TextBox>

        <Border Grid.Column="1"
                Margin="6,0,8,0"
                Padding="12,5"
                VerticalAlignment="Center"
                CornerRadius="999"
                Background="{DynamicResource HoverBrush}">
          <StackPanel Orientation="Horizontal"
                      Spacing="6"
                      VerticalAlignment="Center">
            <ContentControl Width="13"
                            Height="13"
                            VerticalAlignment="Center"
                            Content="{Binding CurrentMode.Key}"
                            ContentTemplate="{Binding CurrentMode.Key, Converter={StaticResource ModeIconTemplateConverter}}" />
            <TextBlock Text="{Binding CurrentMode.Title}"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextSecondaryBrush}" />
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <!-- 词典模式候选/详情浮层：用完即收，不是主窗口固定结构。 -->
    <Popup x:Name="DictionaryPopup"
           IsOpen="{Binding IsDictionaryPopupOpen}"
           PlacementTarget="{Binding ElementName=CapsuleBorder}"
           Placement="Bottom"
           HorizontalOffset="0"
           VerticalOffset="10"
           IsLightDismissEnabled="False">
      <Border Width="640"
             PointerPressed="OnDictionaryPopupPointerPressed"
              Background="{DynamicResource SearchSurfaceBrush}"
              BorderBrush="{DynamicResource SearchSurfaceBorderBrush}"
              BorderThickness="1"
              CornerRadius="18"
              BoxShadow="0 12 28 0 #38000000">
        <Border CornerRadius="18" ClipToBounds="True">
        <Grid>
          <!-- 候选列表 / 空状态提示 -->
          <Grid IsVisible="{Binding IsDictionaryPanelOpen}">
            <TextBlock Text="{Binding DictionaryHint}"
                       IsVisible="{Binding ShowDictionaryHint}"
                       Margin="20,16"
                       FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Top" />

            <ListBox ItemsSource="{Binding DictionaryCandidates}"
                     IsVisible="{Binding HasDictionaryCandidates}"
                     SelectedIndex="{Binding DictionarySelectedIndex, Mode=TwoWay}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="4"
                     MaxHeight="330"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
              <ListBox.Styles>
                <Style Selector="ListBoxItem">
                  <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                  <Setter Property="Padding" Value="12,5" />
                  <Setter Property="Margin" Value="4,1" />
                  <Setter Property="Background" Value="Transparent" />
                  <Setter Property="CornerRadius" Value="8" />
                </Style>
                <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
                  <Setter Property="Background" Value="{DynamicResource HoverBrush}" />
                  <Setter Property="CornerRadius" Value="8" />
                </Style>
                <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
                  <Setter Property="Background" Value="{DynamicResource SelectionBrush}" />
                  <Setter Property="CornerRadius" Value="8" />
                </Style>
              </ListBox.Styles>
              <ListBox.ItemTemplate>
                <DataTemplate x:DataType="vm:DictionaryCandidateViewModel">
                  <StackPanel Spacing="2" PointerPressed="OnDictionaryCandidatePointerPressed">
                    <TextBlock Text="{Binding HeadwordLine}"
                               FontSize="14"
                               FontWeight="600"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding DetailLine}"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                  </StackPanel>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
          </Grid>

          <!-- 详情卡片：不另开系统窗口，保持在当前窗口浮层内。 -->
          <ScrollViewer MaxHeight="400"
                        IsVisible="{Binding IsDictionaryDetailOpen}"
                        HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="18,16" Spacing="10">
              <StackPanel Spacing="2">
                <TextBlock Text="{Binding DictionaryDetailTitle}"
                           FontSize="20"
                           FontWeight="600"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="{Binding DictionaryDetailSubtitle}"
                           IsVisible="{Binding DictionaryDetailHasSubtitle}"
                           FontSize="13"
                           Foreground="{DynamicResource TextSecondaryBrush}" />
              </StackPanel>

              <Border Height="1"
                      Background="{DynamicResource DividerBrush}" />

              <TextBlock Text="{Binding DictionaryDetailDefinitionText}"
                         FontSize="13"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         TextWrapping="Wrap"
                         LineHeight="21" />

              <TextBlock Text="{Binding DictionaryDetailSource}"
                         FontSize="11"
                         Foreground="{DynamicResource TextPlaceholderBrush}" />
            </StackPanel>
          </ScrollViewer>
        </Grid>
        </Border>
      </Border>
    </Popup>

    <!-- 长按 Tab 的模式轮盘：垂直快捷列表，松开 Tab 切换。 -->
    <Popup x:Name="ModeWheelPopup"
           IsOpen="{Binding IsModeWheelOpen}"
           PlacementTarget="{Binding ElementName=CapsuleBorder}"
           Placement="Bottom"
           HorizontalOffset="220"
           VerticalOffset="10"
           IsLightDismissEnabled="False">
      <Border Width="200"
              Padding="4"
              Background="{DynamicResource SearchSurfaceBrush}"
              BorderBrush="{DynamicResource SearchSurfaceBorderBrush}"
              BorderThickness="1"
              CornerRadius="12"
              BoxShadow="0 12 28 0 #38000000"
              PointerWheelChanged="OnModeWheelPointerWheelChanged">
        <Border CornerRadius="12" ClipToBounds="True">
        <ListBox x:Name="ModeWheelList"
                 ItemsSource="{Binding Modes}"
                 SelectedIndex="{Binding ModeWheelSelectedIndex, Mode=TwoWay}"
                 Background="Transparent"
                 BorderThickness="0"
                 Padding="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 Focusable="False">
          <ListBox.Styles>
            <Style Selector="ListBoxItem">
              <Setter Property="HorizontalContentAlignment" Value="Stretch" />
              <Setter Property="Padding" Value="8,6" />
              <Setter Property="Margin" Value="0,1" />
              <Setter Property="Background" Value="Transparent" />
              <Setter Property="CornerRadius" Value="8" />
            </Style>
            <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
              <Setter Property="Background" Value="{DynamicResource HoverBrush}" />
              <Setter Property="CornerRadius" Value="8" />
            </Style>
            <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
              <Setter Property="Background" Value="{DynamicResource SelectionBrush}" />
              <Setter Property="CornerRadius" Value="8" />
            </Style>
          </ListBox.Styles>
          <ListBox.ItemTemplate>
            <DataTemplate x:DataType="modes:SearchMode">
              <StackPanel Orientation="Horizontal"
                          Spacing="8"
                          PointerPressed="OnModeWheelItemPointerPressed">
                <ContentControl Width="13"
                                Height="13"
                                VerticalAlignment="Center"
                                Content="{Binding Key}"
                                ContentTemplate="{Binding Key, Converter={StaticResource ModeIconTemplateConverter}}" />
                <TextBlock Text="{Binding Title}"
                           FontSize="13"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
        </Border>
      </Border>
    </Popup>
  </Panel>
</Window>

